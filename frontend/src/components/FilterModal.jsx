import React from 'react';
import styled from 'styled-components';

const ModalWrapper = styled.div`
  // ... your styles here
`;

const FilterModal = ({ priceRange, setPriceRange, onClose }) => {
  return (
    <ModalWrapper>
      {/* Your filter modal content here */}
    </ModalWrapper>
  );
};

export default FilterModal;
